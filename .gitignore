.env
.env.bridge
**/.env.bridge
*.log
# Output folders the removed ./launch.sh stacks wrote; kept ignored so an
# older checkout never commits them.
metrics_outputs/
logs/
tmp/
data/
.DS_Store
.vscode/
.idea/
__pycache__/
*.pyc
# ScenarioSpec-generated runtime stacks
generated/

# python
.venv

/.mns/
/generated/
/scenarios/*
# The reference campaign is a whole directory:
# the spec, the campaign over it, the route it flies and the estimator
# calibration are one committed unit, because a campaign that ships half of
# itself cannot be copied and run.
!/scenarios/vio-reference/
!/scenarios/vio-reference/**
